<Page
    x:Class="sagittarius.MainPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:sagittarius"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

    <Grid Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">
        <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center">
            <Button x:Name="LoadDirectoryButton" Content="Load Directory" Click="LoadDirectoryButton_Click" Margin="0,0,0,10"/>
            <TextBox x:Name="DirectoryNameTextBox" Header="Directory Name" IsReadOnly="True" Margin="0,0,0,5"/>
            <TextBox x:Name="DirectoryPathTextBox" Header="Directory Path" IsReadOnly="True"/>
        </StackPanel>
    </Grid>
</Page>
